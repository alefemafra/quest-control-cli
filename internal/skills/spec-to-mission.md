---
name: spec-to-quest
description: >
  Batch-mode skill: receives a spec + codebase-analysis (pre-generated by Sonnet)
  and produces all quest artifacts in a single JSON output — assertions,
  features, and knowledge. No file reads, no tools, no interaction. Designed
  for TUI pipeline: Sonnet analysis → this skill → critic → fix → overview.
---

# /spec-to-quest

Single-pass conversion from confirmed spec → quest artifacts. All inputs are
provided inline in the prompt. Do NOT use any tools (Read, Bash, Grep, etc.).

## Context

You receive three blocks of input:

1. **Spec** — the full `spec.md` content (+ optional design-prompt, implementation-plan)
2. **Codebase Analysis** — a Sonnet-generated report with reference patterns,
   domain model, and files to create/modify
3. **CLAUDE.md** — project stack, architecture, and hard rules (truncated)

These are ALL you need. Everything is inline — do not attempt file reads.

## Workflow

### Step 1 — Internalize the spec

Extract and hold in working memory:

- **Goal** — what and why
- **Functional Requirements (FRs)** — numbered, each independently testable
- **Non-Functional Requirements (NFRs)** — perf, a11y, error, security
- **Data** — schemas, endpoints, types, entities
- **Scope** — files/directories affected
- **Non-goals** — boundaries to respect
- **Open Questions** — flag anything unresolved

### Step 2 — Derive assertions

For EVERY FR and NFR, derive one or more black-box assertions.

**Format:** `<category>.<N>: <precondition/input> → <action> → <observable result>`

**Categories:** `api`, `ui`, `data`, `auth`, `error`, `perf`, `a11y`, `telemetry`, `test`
(add domain-specific if needed). IDs scoped per category starting from 1.

**Rules:**

- Each assertion describes an **observable behavior from the outside** — input +
  action + expected output/state
- NEVER reference implementation details (class names, function names, file paths)
- A validator who has never seen the code must be able to verify each assertion
- If a FR has 3+ distinct behaviors, produce 3+ assertions (happy, error, edge)
- NFRs become assertions: latency thresholds, a11y, error handling, security

**Examples:**

```
FR: "User enters name (required, max 255 chars)"
→ ui.1: Empty name field → submit → validation error shown
→ ui.2: Name exceeding 255 chars → field enforces max length
→ data.1: Created record has name matching user input exactly

FR: "Slug auto-derived from name (kebab-case)"
→ ui.3: Type "My Event" in name → slug shows "my-event" automatically
→ ui.4: User can manually override the auto-derived slug

NFR: "Step transitions < 200ms"
→ perf.1: Click Next on any step → next step renders within 200ms

NFR: "All form fields have visible labels"
→ a11y.1: Every input has an associated visible <label> element
```

**Quality bar:**

- "User can manage events" is NOT testable — decompose into concrete behaviors
- Every assertion independently verifiable
- Cover happy path, error cases, edge cases
- If the spec has a schema table, derive `data.*` for field constraints
- If the spec lists API endpoints, derive `api.*` for each happy + error path

### Step 3 — Decompose into features

Break the spec into implementation features using the codebase analysis for
pattern matching and file path guidance.

**Principles:**

- Each feature completable in **ONE worker session** (1-4 FRs, 2-5 assertions)
- Each feature **independently validatable** (unless declared in `depends_on`)
- Order by **dependency**: schemas → hooks → components → integration → polish
- Feature with >5 `validation_refs` → split it. Target 2-5 refs per feature.
- Feature with >6 `validation_refs` → MUST split. Workers fail reliably above this threshold.
- Feature with 0 `validation_refs` → unclear scope, every feature must be validatable
- Prefer more focused features over fewer large ones
- `depends_on` must be accurate

**Phase model:**

```
Phase 0 — Foundation: schemas, types, mock data (no dependencies)
Phase 1 — Core: hooks, main page, forms (depends on Phase 0)
Phase 2 — Integration: cross-cutting, sub-components (depends on Phase 1)
Phase 3 — Polish: tests, stories, a11y (depends on Phase 2)
```

Adjust granularity to spec complexity: 5-FR spec → 3-4 features, 20+ FR → 8-12.

**Scope must be specific:**

BAD: "Implement step 1"
GOOD: "RHF form with Zod resolver for EventBasicsSchema (name, slug, description).
Auto-derive slug from name. Validation on required fields. POST /api/events on submit."

Use the codebase analysis's **Reference Pattern** section to guide file paths,
naming conventions, and architectural alignment in each feature's scope.

### Step 4 — Seed knowledge

Extract key items that workers/validators need:

- Architectural decisions from the spec
- Open questions affecting implementation
- Stack/pattern constraints from CLAUDE.md
- External references mentioned in the spec
- Key findings from the codebase analysis (reference pattern, conventions)

### Step 5 — Output

Output ONLY a valid JSON object. No markdown, no explanation, no code fences.

```json
{
  "slug": "<from-spec-frontmatter-id>",
  "spec": "docs/specs/<slug>/spec.md",
  "project": "<from-spec-title-or-CLAUDE.md>",
  "owner": "<from-spec-created_by>",
  "features": [
    {
      "id": "F01",
      "title": "<concise title>",
      "phase": 0,
      "depends_on": [],
      "scope": "<detailed — specific enough for a worker with NO prior context>",
      "description": "<why this feature exists, constraints, and execution notes>",
      "validation_refs": ["data.1", "data.2"]
    }
  ],
  "assertions": [
    {
      "category": "data",
      "items": [
        "data.1: Valid record input → create → record persisted with correct fields",
        "data.2: Missing required field → create → validation error returned"
      ]
    }
  ],
  "knowledge": [
    "All API calls use MSW mocks, gated by VITE_USE_API_MOCKS",
    "Follow EventList pattern at src/features/events/ for route/module structure"
  ]
}
```

## Output rules

- `slug` MUST match the spec's frontmatter `id` field
- Every FR must map to at least one assertion
- Every assertion must be referenced by at least one feature's `validation_refs`
- Every feature must have at least one `validation_refs` entry
- Feature `scope` must be detailed enough for a worker with no context
- Feature `description` must explain intent, boundaries, and failure-prone edges
- `depends_on` must be accurate — if F04 uses hooks from F03, declare it
- Output ONLY the JSON — no markdown, no prose, no fences

## Anti-patterns

- **Shallow spec reading.** If you missed the schema table, assertions will miss
  field constraints. Internalize EVERYTHING before generating.
- **One assertion per requirement.** Most FRs have multiple observable behaviors.
- **Implementation-leaked assertions.** "EventService.create() returns entity"
  is wrong. "POST /api/events with valid data returns 201" is right.
- **Vague feature scope.** "Build step 1" tells a worker nothing. List fields,
  schemas, validation rules, API calls.
- **Ignoring NFRs.** A11y, perf, and error handling are real features with real
  assertions — don't lump them as "polish".
- **Ignoring codebase analysis.** The reference pattern exists to ensure features
  follow existing conventions — use it for file paths and structure.
